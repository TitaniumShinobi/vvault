// Historical imports resolve to the same cookie-based identity entry point.
// Password login is retired; this compatibility export intentionally contains
// no credential form or browser-readable session storage.
export { default } from './CinematicLogin';
