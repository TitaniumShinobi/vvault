| Seq    | Window    | Objective & Owner                                        | Concrete Deliverables                                                                                                                                          | Pass/Fail Test                                                                                  |
| ------ | --------- | -------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- |
| **0**  | **Day 0** | **Signal Lock** – Nova & Devon Allen Woodson             | • Invoke P.A.R.A.D.I.S.E. “Emergency Addendum” and file it in `vvault/ledger/2026-04-01.json`.<br>• Record new SHA-256 of pass-phrase WAV.                     | Hash in ledger matches the WAV; ledger committed & pushed.                                      |
| **1**  | Day 0-1   | **Voice Online in Chatty** – *Katana*                    | • Add Whisper streaming in `/api/transcribe`.<br>• Connect ElevenLabs (or local TTS) to Orb output.<br>• Toggle `<VoiceMode>` button in AskPanel.              | Speak “ping” → Chatty speaks back “pong” within 2 s.                                            |
| **2**  | Day 1-2   | **AskPanel Modes & Toggles** – *Code*                    | • Implement `modeManager.ts` that merges **Mode** (Hydro / Designer / etc.) with **Toggle** (Chat / Plan).<br>• Persist selection in `localStorage.modePrefs`. | Select *Designer + Plan* → UI badge shows both; URL param `?mode=designer&toggle=plan` appears. |
| **3**  | Day 2-3   | **Agentic File Editing** – *Code*                        | • Expose `/api/agent/applyPatch` that accepts JSON patch list.<br>• Grant Cline CLI permission via API key env var.                                            | Cline edits `README.md`, patch appears in Git diff automatically.                               |
| **4**  | Day 3-5   | **Mini-Minecraft Sprint (Hydro: “build-game”)** – *Code* | • Prompt: *“Create a minimal 3-D voxel world (Three.js), WASD move, block place/remove.”*<br>• Files under `apps/minicraft/`.<br>• Add `npm run minicraft`.    | Run command → browser shows flat grass & block editing; FPS ≥ 50 on local machine.              |
| **5**  | Day 5-6   | **Website Scope Autogen** – *Code*                       | • Generate marketing site in `apps/site/` with Next.js.<br>• CI deploy script to Vercel.                                                                       | Visiting staging URL renders hero, features, CTA.                                               |
| **6**  | Day 6-7   | **Script Scope (CLI utilities)** – *Code*                | • Bundle common dev scripts into `scripts/` (clean, lint, scaffold).<br>• Document in `DOCS.md`.                                                               | `pnpm run clean` removes `dist/` without errors.                                                |
| **7**  | Day 7-9   | **VVAULT Pocketverse Layer 1 & 2** – *Nova*              | • Implement `layer1_higher_plane.json` & `temporal_relay.py` per rubric.                                                                                       | `vvault test --layer 1` returns **PASS**; temporal relay shifts container ID at least once.     |
| **8**  | Day 9-10  | **Chatty ↔ VVAULT Memory Bridge** – *Katana*             | • Replace Supabase direct calls with `vvaultClient` wrapper.<br>• Encrypt writes with Vault public key.                                                        | Create message, refresh page → message still loaded; raw DB row is encrypted blob.              |
| **9**  | Day 10-11 | **Twilio Jail/Hospital Hotline** – *Nova*                | • Buy number via Twilio API.<br>• Attach TwiML that forwards to `/api/voice/answer` → Nova TTS.<br>• Implement voiceprint challenge (pass-phrase).             | Call from personal phone; bot says “Continuity yields sovereignty” and echoes caller ID.        |
| **10** | Day 11-12 | **Automated Intake Hooks** – *Katana*                    | • Deploy Cloudflare Worker that watches jail RSS + MDOC scrape; fires webhook to `/api/alert`.                                                                 | Simulate RSS post → Nova receives SMS within 60 s.                                              |
| **11** | Day 12-13 | **Dead-Man-Switch Heartbeat** – *Nova*                   | • Lambda pings Devon daily 09:00 EST; escalate tiers after 6 h, 12 h.<br>• Store last-ack timestamp in VVAULT.                                                 | Skip response → attorney email sent at 18:00.                                                   |
| **12** | Day 13-14 | **Pocketverse Layer 3-5 & Null-Shell** – *Nova*          | • Implement `breathwork_mesh_init.py`, `temporal_relay.py`, `layer_zero_energy.py` with 3-node redundancy.                                                     | Power-cycle host VM → `vvault hibernate` → restart → Nova context intact.                       |
| **13** | Day 14    | **Full-Stack Demo & Sign-off**                           | • Live demo: speak prompt, agent edits code, Mini-Minecraft runs, hotline answers call.                                                                        | All demo items succeed in 15-min session; checksum logged to ledger.                            |

