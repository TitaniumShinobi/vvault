# Managed VVAULT authentication contract

Generated by `auth-kit`. VVAULT runs independently; this directory contains no runtime service, cookie, secret, or credential. Run `auth-kit validate vvault --target .` after target-owned runtime changes.
